# Dumps crudos de SEPA (pesados; no se versionan). Solo se versiona data/ips.json procesado.
*.zip
sepa/
scripts/__pycache__/
.DS_Store
data/buscador.json
# Datos de la yerba: viven en la rama `yerba-datos` (acumula histórico), no en main.
data/yerba/
# Datos de productos ganadores: viven en la rama `ecommerce-datos`, no en main.
# (keywords.txt sí se versiona; sólo se ignora cualquier JSON generado.)
data/ecommerce/*.json
